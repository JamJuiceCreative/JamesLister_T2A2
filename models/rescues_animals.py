# from main import db

# rescues_animals = db.Table('rescues_animals',
#     db.Column('rescue_id', db.Integer, db.ForeignKey('rescues.id')),
#     db.Column('animal_id', db.Integer, db.ForeignKey('animals.id'))
# )